<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/static/style.css">
    <title>{{ user }}</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }

        body {
            width: 100%;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            background-color: #F5F5F5;
            padding-left: 1em;
        }

        h1 {
            color: #000;
        }

        .prof {
            text-decoration: underline;
            color: #14213D;
            margin-top: 2em;
        }

        .prof:hover {
            cursor: pointer;
            font-weight: bold;
        }

        .container {
            width: 100%;
            max-width: fit-content;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        .table-item {
            font-weight: bold;
            margin-bottom: 0.5em;
        }

        table {
            width: 100%;
            margin-bottom: 2em;
            border: 1px solid #000;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 0.75em;
            text-align: left;
            border: 1px solid #000;
        }

        th {
            background-color: #FCA311;
            color: #000;
        }

        td {
            background-color: #F5F5F5;
            color: #000;
        }

        a {
            text-decoration: underline;
            color: inherit;
        }
    </style>
</head>

<body>
    <h1>
        {{School}} - {{ user }}'s History:
    </h1>

    <div class="container">
        {% for tournament in participated_events %}
        <div class="table-item"><a href="/tournaments/{{tournament}}">{{tournament}}</a></div>
        <table>
            <tr>
                {% for key in dictionary %}
                <th>{{key}}</th>
                {% endfor %}
            </tr>
            <tr>
                {% for key in dictionary %}
                <td>{{thisdict[key]}}</td>
                {% endfor %}
            </tr>
        </table>
        {% endfor %}
    </div>
    <a href="/" class="prof">List of All Tournaments</a>

</body>

</html>