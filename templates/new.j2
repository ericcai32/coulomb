<!DOCTYPE html>

<head>
    <link rel="stylesheet" href="/static/style.css">

    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }

        body {
            width: 100%;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background:
                linear-gradient(to top right, #E76F51 0%, #E9C46A 100%);
        }

        .container {
            max-width: 37em;
            background: #fff;
            border-radius: 0.5em;
            width: 37em;
            padding: 1.25em;
            box-shadow: 0 0.1em .1em 0.5em rgba(0, 0, 0, 0.1);
        }

        #name,
        #check,
        .event_form {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            margin-bottom: 2.5em;
            padding-bottom: 0.625em;
            border-bottom: 0.01em solid #fff;

        }

        .flex {
            display: flex;
            gap: 1.5em;
        }

        .delete {
            text-decoration: none;
            display: inline-block;
            background: #E76F51;
            color: #fff;
            font-size: 1.5rem;
            font-weight: normal;
            width: 1.875em;
            height: 1.875em;
            color: #fff;
            margin-left: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
        }

        .add {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 0.25em;
        }
    </style>


    <title>Add Events</title>
</head>

<body>
    <div class="container">
        <form>
            <span class="add">&plus;</span>
            <label class="tournament_name">Name of the Tournament: </label><input type="text">
            <p class="eventBox">
                <label>Add Event:</label> <input type="text">
                <span class="remove">&times;</span>
            </p>
            <div class="submit">
                <button name="submit" value="SUBMIT">Submit</button>
            </div>
        </form>
    </div>

</body>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
<script src="add_events.js"></script>